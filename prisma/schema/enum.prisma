enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum UserActiveStatus {
    ACTIVE
    BLOCKED
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    INACTIVE
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
    CANCELLED
}

enum PaymentMethod {
    ONLINE
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}